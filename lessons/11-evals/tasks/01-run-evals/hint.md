<details>
<summary>💡 Hint 1: API key not found</summary>

Ensure `ANTHROPIC_API_KEY` is exported in the same terminal session where you run `npm run eval.opus`. Check with `echo $ANTHROPIC_API_KEY`.

</details>

<details>
<summary>💡 Hint 2: Report location</summary>

The report is usually generated in a `dist` or `report` folder. Check the output of `npm run report` for the exact path or URL.

</details>
