## Going further — tests in automation

- Run the quality gates non-interactively in CI with **headless mode** (`claude -p` plus a
  locked-down permission mode).
- Have Claude **review test coverage** and propose the next most valuable test, then add it
  test-first.
- See the **Headless & Automation** task to wrap these checks as project scripts and CI steps.
