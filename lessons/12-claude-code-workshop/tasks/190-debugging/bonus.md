## Going further — navigation & live debugging

- **Code intelligence (LSP)** — jump to definitions, find references, and surface type errors
  instead of scanning files.
- **Chrome (beta)** — debug a live web app, read console logs, and reproduce UI bugs.
- **Computer use** — let Claude drive GUI-only tools from the CLI when a bug lives outside the
  terminal.

Try reproducing a UI bug with a browser-automation skill and comparing it to a code-only
investigation.
