<details>
<summary>💡 Hint 1: Restate the constraint if needed</summary>

If the agent starts down a server-rendered path, restate the client-only constraint — better
early than after a broken build.

</details>

<details>
<summary>💡 Hint 2: Keep the map encapsulated</summary>

Keep it in its own client component; the rest of the app stays server-first.

</details>

<details>
<summary>💡 Hint 3: A picker is just a reporting map</summary>

A location picker is a map that reports back the clicked coordinates — nothing more exotic than
that.

</details>
