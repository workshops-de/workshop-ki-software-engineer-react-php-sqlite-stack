# Assets for Context Engineering slides

Add the context-elements diagram here:

- **Filename:** `context-elements-diagram.png`
- **Content:** The 3×2 grid diagram (System Prompt, Resources, Assistant Messages | Tools, User Message, Tool Call/Response) — e.g. the CleanShot screenshot from the section outline.

The slide references it as `./assets/context-elements-diagram.png`.
